function sum(a,b){
    return a+b;
}

// const value =sum(5,3);
// console.log(value);
        // Assignment
// 1.create a counter in JS (counts down from 30 to 0)
// 2.calculate the time between a setTimeout call and inner fn
//  actually running 
// 3.create a terminal clock(HH: MM : SS)

function countdown(){
    let i = 30;
    while(i>=0){
        
        console.log(i)
        i--;
    }
}
countdown(); 